import type { Contact } from '../../types'

/**
 * Parses the content of a .xlsx file and returns an array of contacts.
 *
 * @param data - The raw ArrayBuffer data of the .xlsx file.
 * @returns An array of parsed Contact objects.
 */
export function parseByXlsx(data: ArrayBuffer): Contact[] {
  // TODO: implement xlsx parsing
  void data
  return []
}
